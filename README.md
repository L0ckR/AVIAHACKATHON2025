# 🏦 ПСБ.Рекомендации (PSB Recommender System)

Система персонализированных продуктовых рекомендаций для клиентов ПСБ Банка. Проект разработан в рамках Авиахакатона 2025.

Решение анализирует профиль клиента и транзакционную активность, чтобы предлагать наиболее релевантные банковские продукты, повышая лояльность и конверсию. Включает в себя аналитический модуль, API сервис и прототип пользовательского интерфейса.

---

## 📂 Структура проекта

```text
AVIAHACKATHON2025/
├── data/                   # Данные и справочники
│   ├── prompts/            # Промпты для LLM (генерация описаний)
│   └── support/            # Вспомогательные JSON (регионы, соцдем кластеры)
├── notebooks/              # Jupyter ноутбуки (DS/ML часть)
│   ├── feature_engineering_psb.ipynb  # Подготовка фичей
│   ├── model.ipynb                    # Обучение модели рекомендаций
│   └── user_profile_analysis.ipynb    # Анализ профилей пользователей
├── web/                    # Веб-сервис и интерфейс (FastAPI)
│   ├── app/                # Бэкенд логика
│   │   ├── main.py         # Точка входа приложения
│   │   ├── routers.py      # API эндпоинты
│   │   ├── services.py     # Бизнес-логика рекомендаций
│   │   └── database.py     # Работа с данными
│   ├── static/             # Фронтенд
│   │   ├── index.html      # Панель менеджера / Демо стенд
│   │   ├── mobile_mockup.html # Прототип мобильного приложения (iOS/Android style)
│   │   └── profile.html    # Детальный профиль клиента
│   ├── psb_products_updated.json # База знаний продуктов банка
│   └── requirements.txt    # Зависимости веб-сервиса
└── s3_script.py            # Скрипт для работы с S3 (загрузка данных)
│
└── parsers_products.py     # Парсеры для продуктов банка (через сайт)
```

---

## 🚀 Основной функционал

1.  **ML-движок рекомендаций**:
    *   Анализ транзакций и соцдем данных.
    *   Кластеризация пользователей ("Семейные реалисты", "Молодые профессионалы" и др.).
    *   Ранжирование банковских продуктов по вероятности отклика.

2.  **Веб-сервис (API + UI)**:
    *   **Панель оператора**: Ввод ID клиента -> Получение списка рекомендаций с объяснением (Match Score).
    *   **Профиль клиента 360°**: Визуализация трат, интересов и текущих продуктов.
    *   **Мобильный прототип**: Демонстрация того, как клиент видит предложения в приложении (Push-уведомления, Stories, Чат).

3.  **User Experience**:
    *   Адаптивный UI в стиле ПСБ.
    *   Интерактивные сценарии (уведомление -> чат -> оформление).

---

## 🛠 Установка и запуск

### Предварительные требования
*   Python 3.8+
*   Установленные библиотеки (см. `requirements.txt` внутри папки `web`)

### Запуск веб-сервиса

1.  Перейдите в директорию веб-приложения:
    ```bash
    cd web
    ```

2.  Установите зависимости:
    ```bash
    pip install -r requirements.txt
    ```

3.  Запустите сервер:
    ```bash
    uvicorn app.main:app --reload
    ```

4.  Откройте в браузере:
    *   **Главная панель**: [http://localhost:8000](http://localhost:8000)
    *   **Мобильный прототип**: [http://localhost:8000/static/mobile_mockup.html](http://localhost:8000/static/mobile_mockup.html)

---

## 📊 Аналитика (Notebooks)

В папке `notebooks/` находится исследовательская часть:
*   Генерация признаков (Feature Engineering).
*   Анализ поведения пользователей.
*   Построение и валидация моделей машинного обучения.
